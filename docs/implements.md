- アドレスバーとかの扱い
-- 100dvh基準でレイアウト確定、下部コントロールにsafe-area-inset-bottomを足す
-- アドレスバー非表示の強制は非推奨、事前に下部余白を確保して崩れ防止

- syncトグル
-- ON: スライド/字幕/音声が同期する
-- OFF: 自動同期なし、字幕タップでページ移動はしない
-- 現状の同期が不安定なので改善が必要

- タイトル、投稿者コンポーネント
-- 固定フッターで試す

- indexボタンの実実装
-- 見た目は現状に合わせる
-- syncのON/OFFに関係なく、Indexからタップされたページにスライドと字幕を切り替える

- 字幕の扱い
-- 1ページあたりの字幕が長い場合やノイズ対策で「全文/AI要約」切替を追加
-- 切替はグローバルでOK、負荷増になる場合は要相談
-- 一旦全文/AI要約」切替はなしで

- ダウンロードボタン
-- Subtitleヘッダー行に左から「Syncトグル / ダウンロード / Index」の順で並べる
-- ボタンの見た目はIndexボタンと揃える
-- ダウンロード対象はスライドPDFのみ

- シークバーとページ数インジケータチップの横幅(優先度低い)
-- いったん保留



- 追加メモ: 質問と回答(今回)
-- Q1: アドレスバー対策は? / A1: 100dvh基準 + safe-area-inset-bottomで崩れ防止、非表示強制はしない
-- Q2: Sync OFF時の字幕タップでページ移動? / A2: 許可しない (今回はUIのみ)
-- Q3: タイトル/著者の位置? / A3: 固定フッターで見やすさ優先
-- Q4: Indexの挙動? / A4: Syncに関係なくタップしたページにスライド/字幕を切替
-- Q5: ダウンロード? / A5: 即ダウンロード、対象はPDFのみ

- 追加メモ: 実装(今回)
-- SubtitleヘッダーにSync/Download/Indexを追加
-- Indexは右スライドインの簡易シートで実装
-- Deckタイトル/著者を固定フッター表示
-- BottomControlsにsafe-area-inset-bottomを加味